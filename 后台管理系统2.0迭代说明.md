# PolicyQuest 后台管理系统 2.0 迭代说明

## 1. 本次后台交付范围

当前版本已经提供 `/admin` 管理后台入口，管理导航按经营链路组织为：

- 经营概览：注册、首卷完成、整卷完成率、AI 调用次数与成本、内容资产、异常状态。
- 用户与权益：用户状态、角色、练习行为、首卷记录；用户 360 档案预留会员、体验额度、积分、订单和申诉位置。
- 真题与材料：省份/地区、年份、卷别、题组、材料、单题、来源和导入状态。
- 题库管理：题目审核、驳回、批量审核和题目详情。
- AI 任务与成本：任务阶段、状态、Token、费用、开始/结束时间、失败原因。
- 批改质检：批改失败、异常低分、待处理单题和人工抽检建议。
- 运营与留存：新人体验、训练提醒、复购/流失及活动成本的承载入口。
- 权限与审计：管理员角色矩阵、敏感操作审批和审计事件承载入口。

本轮明确暂不接入支付、订单、会员、充值包、积分账本和优惠券的真实写入链路。后台对这些指标显示“2.0 待接入”，不使用注册量、充值额或其他近似值冒充现金收入、积分消费或服务利润。

## 2. 经营指标口径

经营概览必须区分以下五类金额，任何统计接口不得只返回一个“收入”字段：

| 指标 | 口径 | 2.0 数据来源 |
| --- | --- | --- |
| 现金收入 | 支付成功且未退款的订单实收，单位为分 | `payment_orders` + `payment_transactions` |
| 积分消费 | 用户消耗积分对应的业务价值，不等价于现金收入 | `points_ledger` |
| 待兑现权益 | 已售出但尚未消耗的会员/服务权益 | `entitlement_grants` |
| 退款 | 已完成退款的订单金额及退款原因 | `refunds` |
| 服务利润 | 现金收入 - 退款 - AI 成本 - 渠道/人工成本 | 财务汇总 + 成本台账 |

产品增长指标建议固定为：注册用户、完成首次练习用户、完成首套真题用户、付费用户、付费转化率、7/30 日留存、复购率和流失率。首卷完成不能用“注册后打开页面”代替，应以一套试卷的提交与批改完成为准。

## 3. 2.0 数据模型建议

### 3.1 套餐与权益

- `product_packages`：套餐主键、套餐类型、展示名称、状态。
- `product_package_versions`：套餐版本、价格分、赠送积分、会员天数、体验额度、开始/结束时间。
- `entitlement_grants`：用户获得的权益、来源订单、原始数量、已消耗数量、冻结数量、失效时间。
- 套餐只允许新增版本，不直接覆盖已售版本；历史订单必须绑定版本号。

### 3.2 订单、支付与退款

- `payment_orders`：业务订单号、用户、套餐版本、应付金额分、实付金额分、订单状态。
- `payment_transactions`：支付渠道、渠道流水号、回调原文摘要、支付状态、到账时间。
- `refunds`：退款单号、订单号、退款金额分、退款原因、审核人与完成时间。
- 回调必须幂等：以渠道流水号和业务订单号建立唯一约束；支付成功和权益发放必须可分别查询、可补偿。
- 所有金额均以“分”存储，前端展示时再换算为元。

### 3.3 积分账本

- `points_accounts`：用户当前可用、冻结、累计发放、累计消费余额的缓存快照。
- `points_ledger`：每笔发放、冻结、结算、释放、退款、到期变化，必须包含变更前余额、变更量、变更后余额、业务编号和操作原因。
- 余额只能由账本事务计算产生，禁止只修改余额字段而不写账本。
- 人工改积分必须填写原因，涉及超过阈值的变更必须二次审批。

### 3.4 AI 任务与成本

现有 `ai_tasks` 已能承载基础任务记录。2.0 建议补充：

- `request_id`、`business_type`、`business_id`、`stage`、`model`、`provider`。
- `input_tokens`、`output_tokens`、`total_tokens`、`unit_price`、`cost`、`latency_ms`、`retry_count`、`retry_reason`。
- 业务编号必须能从“某套卷/某道题亏了多少钱”反查到具体模型调用。

### 3.5 批改质检与审计

- `grading_appeals`：用户申诉、申诉原因、处理状态、处理人、处理结论。
- `grading_quality_flags`：异常高低分、重复扣分、范文字数、证据错误等质检标签。
- `grading_revisions`：修订前 JSON、修订后 JSON、修订原因、修订人、修订时间。
- `admin_audit_logs`：操作人、角色、动作、对象、前后版本、业务编号、原因、审批状态、IP 和时间。

## 4. 迭代优先级

### P0：财务与权益底座

1. 建立套餐版本、订单、支付流水、退款和积分账本。
2. 接入支付回调幂等、到账补偿、退款反向记账。
3. 将经营概览的现金收入、积分消费、待兑现权益、退款和利润切换为真实口径。

### P1：用户与运营闭环

1. 用户 360 接入会员、权益余额、积分流水、订单和申诉。
2. 新人体验、优惠券、训练提醒和沉默用户分群。
3. 每个活动关联付费归因、权益成本、AI 成本和活动净贡献。

### P2：质检与治理

1. 申诉工单、人工抽检、修订版本和证据校验。
2. 客服、教研、财务角色分权和敏感操作审批。
3. 完成订单补单、退款、改积分、改分和权限变更的审计闭环。

## 5. 验收标准

- 修改套餐价格不会改变历史订单权益。
- 支付成功、积分到账、权益发放均可由业务编号追溯。
- 同一支付回调重复到达不会重复发放权益或积分。
- 每笔积分变化都有账本记录和原因，人工调整有审批记录。
- 可以按用户、订单、套卷、题目和 AI `request_id` 反查成本。
- 批改修订可查看修订前后版本，申诉有处理结论。
- 财务指标能够区分现金收入、积分消费、待兑现权益和退款，不以充值额替代利润。
- 客服、教研、财务无法越权执行对方的敏感操作。

## 6. 当前版本的边界

本轮为了不在没有真实订单/支付数据模型的情况下制造财务数据，后台已完成信息架构、真实学习数据接入和 2.0 待接入提示。支付、订单、会员、积分和优惠券应在完成上述底座设计、迁移脚本、回调幂等与审计后再开放写入。
